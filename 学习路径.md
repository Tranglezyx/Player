# 微信小游戏开发学习路径

> 基于当前项目（官方快速开始示例 — 飞机射击游戏）的源码阅读顺序

---

## 第一层：入口 & 配置（理解项目骨架）

| 序号 | 文件 | 要点 |
|------|------|------|
| 1 | [game.json](Player/game.json) | 游戏运行时配置，此处仅设了竖屏 `"deviceOrientation": "portrait"` |
| 2 | [project.config.json](Player/project.config.json) | IDE/编译配置，注意 `"compileType": "game"` 区分小游戏和小程序 |
| 3 | [game.js](Player/game.js) | 项目真正的入口文件，仅 3 行：`import Main` 然后 `new Main()` |

---

## 第二层：Canvas 初始化（理解渲染基础）

| 序号 | 文件 | 要点 |
|------|------|------|
| 4 | [js/render.js](Player/js/render.js) | `wx.createCanvas()` 创建画布，`wx.getWindowInfo()` 获取屏幕尺寸。**与浏览器游戏最大不同**：没有 DOM，所有 API 都是 `wx.*` 前缀 |

---

## 第三层：游戏主循环（核心骨架）

| 序号 | 文件 | 要点 |
|------|------|------|
| 5 | [js/main.js](Player/js/main.js) | `Main` 类，包含游戏标准三板斧：`loop()` 帧循环驱动、`update()` 更新逻辑（生成敌人、碰撞检测）、`render()` 绘制（清画布 → 画背景 → 画实体 → 画 UI） |

---

## 第四层：状态管理 & 基础类

| 序号 | 文件 | 要点 |
|------|------|------|
| 6 | [js/databus.js](Player/js/databus.js) | 全局状态单例，管理分数、帧计数、敌人/子弹/动画数组、对象池 |
| 7 | [js/base/sprite.js](Player/js/base/sprite.js) | 所有游戏对象的基类，封装 `wx.createImage()` 加载图片 + `drawImage` 绘制 + 矩形碰撞检测 |
| 8 | [js/base/animation.js](Player/js/base/animation.js) | 继承 Sprite，实现帧动画（爆炸效果），用 `setInterval` 逐帧切换图片 |
| 9 | [js/base/pool.js](Player/js/base/pool.js) | 对象池，避免频繁 `new` / GC，游戏性能优化的关键模式 |

---

## 第五层：游戏实体

| 序号 | 文件 | 要点 |
|------|------|------|
| 10 | [js/player/index.js](Player/js/player/index.js) | 玩家类，处理触摸拖动（`wx.onTouchStart/Move/End`）、每 20 帧自动射击 |
| 11 | [js/player/bullet.js](Player/js/player/bullet.js) | 子弹类，每帧向上移动，出屏后回收到对象池 |
| 12 | [js/npc/enemy.js](Player/js/npc/enemy.js) | 敌机类，每帧向下移动，被击中播放爆炸帧动画后回收 |

---

## 第六层：辅助系统

| 序号 | 文件 | 要点 |
|------|------|------|
| 13 | [js/runtime/background.js](Player/js/runtime/background.js) | 双图拼接实现无限滚动背景 |
| 14 | [js/runtime/music.js](Player/js/runtime/music.js) | `wx.createInnerAudioContext()` 管理背景音乐和音效 |
| 15 | [js/runtime/gameinfo.js](Player/js/runtime/gameinfo.js) | 分数 UI 和 Game Over 界面，从图集（Common.png）中裁切按钮图，用 EventEmitter 派发重启事件 |

---

## 建议阅读顺序

```
game.json
  → project.config.json
    → game.js
      → js/render.js
        → js/main.js
          → js/databus.js
            → js/base/sprite.js
            → js/base/animation.js
            → js/base/pool.js
          → js/player/index.js
          → js/player/bullet.js
          → js/npc/enemy.js
        → js/runtime/background.js
        → js/runtime/music.js
        → js/runtime/gameinfo.js
```

总共约 **500 行代码**，按此顺序通读一遍即可掌握微信小游戏的完整开发模式。
