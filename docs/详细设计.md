# 详细设计

> 游戏核心流程、数据流、状态的实现细节

## 1. 游戏启动流程

```
game.js
  └→ new Main()
       ├→ render.js 初始化 Canvas + 安全区域（SAFE_TOP / SAFE_BOTTOM）
       ├→ new DataBus() 全局状态
       ├→ new Cultivator() 修炼者
       ├→ new CombatSystem() 战斗
       ├→ new CultivationSystem() 修炼
       ├→ new SkillSystem() 功法
       ├→ new ShopSystem() 商店
       ├→ new TopBar / BottomBar / Panels
       ├→ loadSaveData() 读档
       ├→ calculateOfflineRewards() 离线结算
       └→ this.loop() 启动主循环
```

## 2. render.js 安全区域

```js
// js/render.js
const windowInfo = wx.getWindowInfo ? wx.getWindowInfo() : wx.getSystemInfoSync();
const statusBarHeight = windowInfo.statusBarHeight || 48;

// 底部安全距离：全面屏（刘海屏）留 24px，普通屏留 8px
const isNotchScreen = statusBarHeight >= 40;
export const SAFE_BOTTOM = isNotchScreen ? 24 : 8;

export const SAFE_TOP = statusBarHeight + 4;
export const TOP_BAR_H = 60;
export const BOTTOM_BAR_H = 66;

// 面板：高度为中间区域的 75%，垂直居中
const middleArea = screenHeight - (SAFE_TOP + TOP_BAR_H + 2) - BOTTOM_BAR_H - SAFE_BOTTOM;
export const PANEL_H = Math.floor(middleArea * 0.75);
export const PANEL_Y = Math.floor(SAFE_TOP + TOP_BAR_H + 2 + (middleArea - PANEL_H) / 2);
```

## 3. 游戏主循环

```js
class Main {
  constructor() {
    this.initSystems();
    this.initUI();
    this.loadGame();
    this.bindEvents();
    this.loop();
  }

  update(dt) {
    bus.frame++;
    // 1. 修炼 tick
    bus.cultivationSystem.update(dt);
    // 2. 战斗系统
    bus.combatSystem.update(dt);
    // 3. 商店刷新
    bus.shopSystem.update(dt);
    // 4. 实体动画（呼吸、粒子）
    bus.cultivator.update(dt);
    bus.combatSystem.currentMonster?.update(dt);
    // 5. 浮动文字
    this.floatingText.update(dt);
    // 6. 每分钟收益
    this.topBar.update();
    // 7. 自动存档（每 600 帧 ≈ 10 秒）
    if (bus.frame % 600 === 0) saveToStorage();
  }

  render() {
    ctx.clearRect(0, 0, canvas.width, canvas.height);
    // 背景 + 地面
    // 战斗（怪物 + 伤害数字）
    bus.combatSystem.render(ctx);
    // 修炼者
    bus.cultivator.render(ctx);
    // 浮动文字
    this.floatingText.render(ctx);
    // 离线弹窗（如需要）
    // 顶部栏 / 底部栏 / 面板
  }
}
```

### 交互事件

```js
// 触摸处理优先级：
// 1. 离线收益弹窗 → 领取关闭
// 2. 顶部栏地图名区域 → 弹出地图面板
// 3. 底部按钮栏 → 切换面板（点击同一按钮关面板）
// 4. 已打开的面板 → 面板内部处理（突破/购买/切换地图等）
//    点击面板外部 → 关闭面板
```

## 4. 数据总线（DataBus）扩展

基于现有 `js/databus.js` 扩展：

```js
class DataBus {
  // === 复用现有 ===
  frame = 0;
  pool = new Pool();
  isGameOver = false;

  // === 新增：实体列表 ===
  monsters = [];        // 当前场景怪物
  damageNumbers = [];   // 浮动伤害数字

  // === 新增：修炼者数据 ===
  cultivator = null;    // 修炼者实例引用

  // === 新增：修炼状态 ===
  cultivationPaused = false;  // 修炼是否暂停（灵石不足）

  // === 新增：每分钟收益 ===
  stonePerMin = 0;         // 灵石/分钟
  cultivationPerMin = 0;   // 修为/分钟

  // === 新增：系统实例 ===
  combatSystem = null;
  cultivationSystem = null;
  realmSystem = null;
  skillSystem = null;
  shopSystem = null;

  reset() {
    this.frame = 0;
    this.monsters = [];
    this.damageNumbers = [];
    this.cultivationPaused = false;
    this.stonePerMin = 0;
    this.cultivationPerMin = 0;
  }
}
```

## 5. 修炼系统 Ticker 设计

修炼每秒执行一次（每秒消耗灵石 → 获得修为）：

```js
class CultivationSystem {
  timer = 0;
  TICK_INTERVAL = 1; // 秒

  update(deltaTime) {
    this.timer += deltaTime;
    if (this.timer < this.TICK_INTERVAL) return;
    this.timer = 0;

    const player = GameGlobal.databus.cultivator;
    if (!player.autoCultivate) return;

    // 修为已满且小等级=10 → 等待手动突破
    if (player.cultivation >= player.maxCultivation) {
      if (player.level < 10) {
        this.levelUp(player); // 自动升小等级
      }
      return; // level=10 时等待突破
    }

    // 计算灵石消耗（stoneCostPerSec 读取自当前境界配置）
    const realm = GameGlobal.databus.realmSystem.config[player.realmIndex];
    const stoneCost = player.cultivationSpeed * realm.stoneCostPerSec;
    if (player.spiritStone < stoneCost) {
      GameGlobal.databus.cultivationPaused = true;
      return;
    }

    // 消耗灵石，增加修为
    player.spiritStone -= stoneCost;
    player.cultivation += player.cultivationSpeed;

    // 灵力自动回复
    if (player.spirit < player.maxSpirit) {
      player.spirit = Math.min(player.maxSpirit, player.spirit + player.spiritRegen);
    }

    // 修炼中积累心法熟练度
    player.innerSkills.forEach(skill => {
      skill.proficiency += 1;
      checkProficiencyLevelUp(skill);
    });
  }

  levelUp(player) {
    player.level += 1;
    player.cultivation = 0;
    player.maxCultivation = this.calcCultivationRequired(player.realmIndex, player.level);
    this.updatePlayerStats(player); // 按小等级更新属性
  }
}
```

## 6. 战斗系统设计

```js
class CombatSystem {
  constructor() {
    this.monsterSpawnTimer = 0;
    this.monsterRespawnCooldown = 2; // 死亡后 2 秒重生
    this.currentMonster = null;
  }

  update(deltaTime) {
    this.updateMonsterSpawn();
    this.updateCombat();
    this.updateDamageNumbers();
  }

  updateMonsterSpawn() {
    // 没有怪物时生成新怪物
    if (!this.currentMonster || !this.currentMonster.isActive) {
      this.monsterSpawnTimer += deltaTime;
      if (this.monsterSpawnTimer >= this.monsterRespawnCooldown) {
        this.monsterSpawnTimer = 0;
        this.spawnMonster();
      }
    }
  }

  spawnMonster() {
    const monster = GameGlobal.databus.pool.getItemByClass('monster', Monster);
    monster.init(GameGlobal.databus.cultivator.realmIndex);
    GameGlobal.databus.monsters.push(monster);
    this.currentMonster = monster;
  }

  updateCombat() {
    if (!this.currentMonster || !this.currentMonster.isActive) return;

    const player = GameGlobal.databus.cultivator;

    // 普通攻击计时
    player.attackTimer += deltaTime;
    if (player.attackTimer >= player.attackSpeed) {
      player.attackTimer = 0;
      this.performNormalAttack(player, this.currentMonster);
    }

    // 主动术法：灵力足够就释放，无 CD 限制
    player.outerSkills
      .filter(s => s.type === 'active')
      .forEach(skill => {
        if (player.spirit >= skill.spiritCost) {
          player.spirit -= skill.spiritCost;
          this.performSkillAttack(player, this.currentMonster, skill);
        }
      });
  }

  performNormalAttack(player, monster) {
    const damage = this.calcDamage(player, monster, 1);
    monster.hp -= damage;
    this.spawnDamageNumber(monster.x, monster.y, damage, false);
    player.state = 'attack';
    this.checkMonsterDeath(monster);
  }

  performSkillAttack(player, monster, skill) {
    const damage = this.calcDamage(player, monster, skill.getEffect());
    monster.hp -= damage;
    this.spawnDamageNumber(monster.x, monster.y, damage, false, 'skill');
    skill.proficiency += 1; // 术法熟练度
    checkProficiencyLevelUp(skill);
    this.checkMonsterDeath(monster);
  }

  calcDamage(attacker, defender, multiplier) {
    const base = Math.floor(attacker.attack ** 2 / (attacker.attack + defender.defense));
    let damage = Math.floor(base * multiplier);

    // 暴击
    if (Math.random() < attacker.critRate) {
      damage *= attacker.critDamage;
      // 标记为暴击，DamageNumber 渲染时用黄色
    }
    return Math.floor(damage);
  }

  checkMonsterDeath(monster) {
    if (monster.hp <= 0) {
      monster.isActive = false;
      monster.state = 'dead';

      // 发放奖励
      const player = GameGlobal.databus.cultivator;
      player.spiritStone += monster.stoneReward;
      player.spirit = Math.min(player.maxSpirit, player.spirit + 5); // 击杀回复少量灵力

      // 掉落装备和残卷（直接进背包）
      this.rollEquipmentDrop(monster);
      this.rollFragmentDrop(monster);

      // 回收怪物
      GameGlobal.databus.pool.recover('monster', monster);
      GameGlobal.databus.totalKills++;
      this.currentMonster = null;
    }
  }
}
```

## 7. 商店系统设计

```js
class ShopSystem {
  REFRESH_INTERVAL = 4 * 3600; // 4 小时（现实时间）
  timer = 0;
  items = [];        // 当前商品列表

  update(deltaTime) {
    this.timer += deltaTime;
    if (this.timer >= this.REFRESH_INTERVAL) {
      this.autoRefresh();
    }
  }

  autoRefresh() {
    this.timer = 0;
    const player = GameGlobal.databus.cultivator;
    const slotCount = player.getGlobalLevel(); // 人物等级 = 商品数

    // 保留锁定的商品
    const locked = this.items.filter(item => item.locked);
    const freeSlots = slotCount - locked.length;

    // 在空闲槽位生成新商品
    const newItems = [];
    for (let i = 0; i < freeSlots; i++) {
      newItems.push(this.generateItem(player.realmIndex));
    }

    this.items = [...locked, ...newItems];
  }

  manualRefresh() {
    const cost = this.getRefreshCost();
    const player = GameGlobal.databus.cultivator;
    if (player.spiritStone < cost) return;
    player.spiritStone -= cost;
    this.autoRefresh();
  }

  getRefreshCost() {
    return 50 * (GameGlobal.databus.cultivator.realmIndex + 1);
  }

  toggleLock(index) {
    this.items[index].locked = !this.items[index].locked;
  }

  buy(index) {
    const item = this.items[index];
    const player = GameGlobal.databus.cultivator;
    if (player.spiritStone < item.price) return false;

    player.spiritStone -= item.price;
    // 物品进背包 / 功法习得
    if (item.type === 'equipment') {
      addToBag(item.data);
    } else if (item.type === 'skill_fragment') {
      addFragment(item.data);
    } else if (item.type === 'skill_complete') {
      learnSkill(item.data);
    }

    this.items.splice(index, 1); // 购买后移除
    return true;
  }

  generateItem(realmIndex) {
    // 按权重随机生成：装备/术法残卷/心法残卷/完整功法
    const roll = Math.random() * 100;
    if (roll < 50) return this.genEquipment(realmIndex);
    if (roll < 75) return this.genSkillFragment('outer');
    if (roll < 90) return this.genSkillFragment('inner');
    return this.genCompleteSkill();
  }
}
```

## 8. 存档与读档

```js
const SAVE_KEY = 'cultivation_idle_save';

function saveToStorage() {
  const player = GameGlobal.databus.cultivator;
  const data = {
    realmIndex: player.realmIndex,
    cultivation: player.cultivation,
    maxCultivation: player.maxCultivation,
    spiritStone: player.spiritStone,
    equipment: player.equipment,
    innerSkills: player.innerSkills.map(s => ({
      id: s.id, proficiency: s.proficiency, proficiencyLevel: s.proficiencyLevel,
    })),
    outerSkills: player.outerSkills.map(s => ({
      id: s.id, proficiency: s.proficiency, proficiencyLevel: s.proficiencyLevel,
    })),
    lastOnlineTime: Date.now(),
    totalKills: GameGlobal.databus.totalKills,
  };
  wx.setStorageSync(SAVE_KEY, data);
}

function loadSaveData() {
  const data = wx.getStorageSync(SAVE_KEY);
  if (!data) return null; // 新玩家

  // 恢复修炼者状态
  const player = GameGlobal.databus.cultivator;
  player.realmIndex = data.realmIndex;
  player.cultivation = data.cultivation;
  player.spiritStone = data.spiritStone;
  // ... 恢复装备、心法、术法

  // 计算离线收益
  if (data.lastOnlineTime) {
    const offlineSec = (Date.now() - data.lastOnlineTime) / 1000;
    calculateOfflineRewards(offlineSec);
  }

  return data;
}
```

## 9. 离线收益计算

```js
function calculateOfflineRewards(offlineSec) {
  // 无离线系数，满额收益
  const offlineMin = Math.floor(offlineSec / 60);
  const player = GameGlobal.databus.cultivator;

  // 每分钟修炼收益（与在线一致）
  const cultivationPerMin = player.cultivationSpeed * 60;
  const cultivationGain = cultivationPerMin * offlineMin;

  // 每分钟战斗灵石收益（stoneCostPerSec 读取自当前境界配置）
  const realm = GameGlobal.databus.realmSystem.config[player.realmIndex];
  const combatStonePerMin = player.cultivationSpeed * realm.stoneCostPerSec * 3 * 60;
  const stoneGain = combatStonePerMin * offlineMin;

  // 扣除修炼灵石消耗
  const cultivationCost = player.cultivationSpeed * realm.stoneCostPerSec * offlineSec;
  const netStone = Math.max(0, stoneGain - cultivationCost);

  return {
    totalMin: offlineMin,
    cultivation: cultivationGain,
    stone: netStone,
    stoneSpent: cultivationCost,
  };
}
```

## 10. 数值格式化（分位数体系）

内部计算统一用 JS `Number`（当前数值上限 ~千万，远小于 `2^53` 安全线），显示时自动分位。

```
分位单位（万进制，每 4 位一级）：
  个 (< 10000) → 万 (10^4) → 亿 (10^8) → 兆 (10^12) → 京 (10^16) → 垓 (10^20) → ...

规则：
  - 数值 < 10000：直接显示整数
  - 数值 >= 10000：取高位 + 单位，保留 1 位小数
  - 整数部分 < 10 时保留 1 位小数，>= 10 时取整

显示示例：
  1234        → 1234
  12345       → 1.2万
  987654      → 98.8万
  123456789   → 1.2亿
  99999999999 → 999.9亿
```

```js
// js/utils/number.js
const UNITS = ['', '万', '亿', '兆', '京', '垓', '秭', '穰', '沟', '涧', '正', '载'];
const BASE = 10000;

class BigNumber {
  constructor(v) { this.value = Math.floor(v); }

  // 加法（链式调用）
  add(n) { this.value += n; return this; }
  sub(n) { this.value -= n; return this; }
  mul(n) { this.value = Math.floor(this.value * n); return this; }

  toString() {
    if (this.value < BASE) return String(this.value);
    let v = this.value;
    let idx = 0;
    while (v >= BASE && idx < UNITS.length - 1) {
      v /= BASE;
      idx++;
    }
    return (v < 10 ? v.toFixed(1) : String(Math.floor(v))) + UNITS[idx];
  }

  toNumber() { return this.value; }
}
```

> 为什么不直接用 `BigInt`？当前版本数值上限约数千万，`Number` 安全线 ~9 千万亿，绰绰有余。后期若数值膨胀到京级（10^16）以上再迁移。分位显示已预留到"载"（10^44）。

### 收益显示适配

顶部资源栏同步使用分位格式：

```
灵石: 1.2万  (+180/分钟)
修为: 67%  (+12/分钟)
```

掉落浮动数字也使用分位：
```
-1.5万  （暴击金色）
-2300   （普通白色）
```

---

## 11. 事件总线

基于现有 `tinyemitter.js`：

| 事件名 | 触发时机 | 监听方 |
|--------|---------|--------|
| `monster_killed` | 怪物死亡 | UI、修炼系统 |
| `cultivation_tick` | 每秒修炼 | UI、心法系统 |
| `breakthrough` | 境界突破 | UI、人物、战斗 |
| `equipment_dropped` | 装备掉落 | UI 弹窗 |
| `skill_level_up` | 熟练度晋级 | UI 弹窗 |
| `offline_reward` | 离线结算完成 | UI 弹窗 |
| `shop_refresh` | 商店自动刷新 | UI 更新 |
| `shop_buy` | 购买商品 | UI、背包、功法 |
